# Bedtime Stories - Cursor AI Rules

Follow these patterns for consistency. See `V0_INTEGRATION.md` for UI redesign workflow.

## 🏗️ Architecture

### Tech Stack
- Next.js 16.1.6 App Router + TypeScript
- Tailwind CSS v4 (default palette only)
- Supabase Auth (@supabase/ssr)
- Cloudflare R2 (AWS SDK v3)

### Structure
```
src/
├── app/              # Pages (orchestration only)
├── components/       # UI only (no DB/API calls)
├── lib/
│   ├── actions/      # Server Actions (mutations)
│   ├── api/          # API client utilities
│   ├── queries/      # Database reads
│   └── supabase/     # Client helpers
```

### Separation of Concerns
- **Pages**: Fetch data → Pass to components
- **Components**: Render UI only (data from props)
- **lib/actions**: Mutations (create, update, delete)
- **lib/queries**: Database reads
- **lib/api**: Client-side API calls

## 🔐 Authentication

**No Middleware** - Auth checks at page/route level (avoids Edge Runtime issues).

### Protected Pages
```typescript
import { requireAuth } from '@/lib/auth';
import { getStories } from '@/lib/queries/stories';
import StoriesList from '@/components/StoriesList';

export default async function Home() {
  const user = await requireAuth();
  const { stories } = await getStories();
  return <StoriesList stories={stories} userEmail={user.email} />;
}
```

### Protected API Routes
```typescript
import { requireAuthAPI } from '@/lib/auth';

export async function GET() {
  const { error, user } = await requireAuthAPI();
  if (error) return error;
  // ... protected logic
}
```

### Auth Forms (Login/Signup)
Page wrappers must be client components to pass functions:

```typescript
// app/login/page.tsx
'use client';

import { login } from '@/lib/actions/auth';
import LoginForm from '@/components/LoginForm';

export default function LoginPage() {
  async function handleLogin(email: string, password: string) {
    const result = await login(email, password);
    if (result?.error) throw new Error(result.error);
  }
  return <LoginForm onSubmit={handleLogin} />;
}
```

### ❌ NEVER Use Supabase Clients Directly

**Only allowed in:**
- `lib/actions/*`, `lib/queries/*`, `lib/auth.ts`

**Never in:**
- `components/*` (use props/callbacks)
- `app/*` pages (use query functions/actions)

## 🎨 Design System

### Ocean Theme - Calm & Warm
**Tone:** Calm, gentle, ocean-inspired, toddler-friendly (not childish), emotionally reassuring

**Colors (Default Tailwind only):**
- Background: `bg-slate-50`
- Headings: `text-blue-950` with `font-extrabold tracking-tight`
- Primary buttons: `bg-teal-500 hover:bg-teal-400 text-white font-bold`
- Links: `text-teal-600 hover:text-teal-500`
- Badges: `bg-teal-50 text-teal-700 font-semibold`
- Success: `bg-emerald-50 text-emerald-800`
- Error: `bg-red-50 text-red-800`
- Cards: `bg-white shadow-sm`
- Accents: teal-300, teal-400, teal-500

**Rounded Corners:**
- Cards/containers: `rounded-3xl`
- Buttons/inputs: `rounded-2xl`
- Badges: `rounded-full`
- Video player: `rounded-2xl`

**Spacing:**
- Generous padding: `p-6 sm:p-10` for cards
- Large tap targets: `py-3.5 px-6` for buttons
- Breathable gaps: `gap-6`, `space-y-6`

**Typography:**
- Headings: `font-extrabold text-3xl sm:text-4xl tracking-tight`
- Body: `text-base leading-relaxed`
- Labels: `font-semibold text-sm`
- Use `text-pretty` or `text-balance` for headings

**Interactive States:**
- Hover: `transition-colors` or `transition-all`, subtle scale: `hover:scale-[1.02]`
- Focus: `focus-visible:ring-2 focus-visible:ring-teal-500 focus-visible:ring-offset-2`
- Disabled: `disabled:opacity-50`
- Shadows: `shadow-sm hover:shadow-lg`

**Form Inputs:**
- Border: `border-2 border-slate-200`
- Focus: `focus:border-teal-500 focus:ring-0`
- Size: `px-4 py-3.5 text-base`

**Visual Identity Elements:**
- **Gradient Accent:** Add to all major cards
  ```tsx
  <div className="absolute inset-x-0 top-0 h-1.5 bg-gradient-to-r from-teal-400 via-teal-500 to-blue-500" />
  ```
  Use on: StoryCard, upload form, story detail, etc.

- **Page Header Hierarchy:** Use on all pages except home
  ```tsx
  <p className="text-xs font-semibold uppercase tracking-widest text-teal-500">
    Across the Seas
  </p>
  <h1 className="mt-2 text-3xl font-extrabold tracking-tight text-blue-950 sm:text-4xl">
    Page Title
  </h1>
  <p className="mt-3 text-base leading-relaxed text-slate-500">
    Subtitle or description
  </p>
  ```

- **Back Link:** Consistent placement and style
  ```tsx
  <Link
    href="/"
    className="inline-flex items-center gap-2 text-sm font-semibold text-teal-600 transition-colors hover:text-teal-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-teal-500 focus-visible:ring-offset-2"
  >
    ← Back to Stories
  </Link>
  ```

**Reusable Components:**
- `StoryCard` - Standard card with gradient, badge, title, date

### Rules
- ✅ Default Tailwind palette only
- ✅ No dark mode
- ✅ Mobile-first, large tap targets
- ✅ Calm, breathable spacing
- ❌ No bright primary colors, no hard shadows
- ❌ Never extend tailwind config

## 📝 TypeScript

- Use `type` keyword for type-only imports
- Import shared types from `@/lib/types`
- Type all event handlers: `FormEvent<HTMLFormElement>`
- Use `Readonly<>` for props when appropriate

## 🔧 Environment Variables

Required in `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx
R2_ACCOUNT_ID=xxx
R2_ACCESS_KEY_ID=xxx
R2_SECRET_ACCESS_KEY=xxx
R2_BUCKET=bedtime-stories
```

- Use `sb_publishable_*` format (not legacy `eyJ...`)
- Never commit `.env.local`
- Never use `NODE_TLS_REJECT_UNAUTHORIZED=0` (turn off Zscaler)

## 📦 Imports

- Always use `@/` alias for src imports
- Import order: Next/React → Third-party → Local utils → Components → Types

## 🚫 Anti-Patterns

### ❌ No Middleware
- Causes Edge Runtime fetch issues in Next.js 16

### ❌ No Business Logic in Components
```typescript
// ❌ BAD
const supabase = createClient(); // In component

// ✅ GOOD
interface Props { stories: Story[] }
export default function MyComponent({ stories }: Props) { ... }
```

### ❌ No Direct Database Queries in Pages
```typescript
// ❌ BAD
const { data } = await supabase.from('stories').select('*');

// ✅ GOOD
const { stories } = await getStories();
```

## ✅ Principles

### DRY
- Use helpers: `requireAuth()`, `getStories()`, etc.
- No repeated auth checks or queries

### Separation
- Pages: Orchestrate (fetch → pass props)
- Components: UI only (no fetching/DB)
- lib/: All business logic

### Security
- `requireAuth()` for protected pages
- `requireAuthAPI()` for API routes
- Validate inputs in API routes
- RLS enabled in Supabase

### Performance
- Server components for data fetching
- Minimize client JS

## 📚 Key Files

### Business Logic
- `lib/auth.ts` - requireAuth(), requireAuthAPI()
- `lib/actions/` - Server actions (login, signup, createStoryRecord)
- `lib/queries/` - DB reads (getStories, getStoryById)
- `lib/api/` - API utils (getVideoUrl, getUploadUrl, uploadToR2)
- `lib/r2.ts` - R2 signed URLs
- `lib/types.ts` - Shared types

### UI (components/)
Pure presentational components:
- `StoryCard` - Reusable card (gradient top, badge, title, date)
- `LoginForm`, `SignupForm` - Auth forms
- `StoriesList`, `StoryDetail` - Story views
- `VideoPlayer` - Video display

### Pages (app/)
- Orchestrators (fetch → render component)
- No business logic

### API Routes (app/api/)
- `upload-url/route.ts` - R2 upload URL
- `video-url/route.ts` - R2 video URL

## 🎯 Quick Patterns

### Add Protected Page
```typescript
// app/new-page/page.tsx
import { requireAuth } from '@/lib/auth';
import { getData } from '@/lib/queries/data';
import MyComponent from '@/components/MyComponent';

export default async function Page() {
  await requireAuth();
  const { data } = await getData();
  return <MyComponent data={data} />;
}
```

### Add Form
1. Server action in `lib/actions/`
2. UI component in `components/` (receive onSubmit prop)
3. Page wrapper passes action as callback

### Redesign with v0
Read `V0_INTEGRATION.md` → Keep props interface → Replace component

## 📌 Core Rules

- **No middleware** (Edge Runtime issues)
- **No Supabase in components** (use actions/queries)
- **No fetching in pages** (use query functions)
- **Props over fetching** (pass data down)
- **Ocean theme only** (blue-950, teal-500, slate)
- **Server > Client** (prefer server components)
